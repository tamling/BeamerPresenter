// Build stamp. Overwritten at build time by build-app.sh (macOS); the committed
// values are a fallback for `swift run` and fresh checkouts.
enum BuildInfo {
    static let date = "2026-06-21"
    static let commit = "644404c"
}
